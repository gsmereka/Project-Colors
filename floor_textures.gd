extends Node2D


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	#self.material.set_shader_parameter("color", Color(1,0,1,1))
	#print(self.material.get_shader_parameter("color"))
	#var node = self.modulate
	#self.get_node("House_normal2").modulate.g = 0
	#print(self.get_node("House_normal2").modulate)
	pass
